<h1>Tasks</h1>

<ul>
  <% @tasks.each do |t| %>
    <li><%= link_to t.title, task_path(t) %><%= link_to 'delete',  task_path(t), method: :delete, data: { confirm: 'are you sure?' } %></li>
  <% end %>
</ul>

<%= link_to 'new task', new_task_path %>
